name: CI

"on":
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

env:
  PYTHONUTF8: "1"

jobs:
  quality-gates:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      UV_PYTHON: "3.11"
    steps:
      - uses: actions/checkout@v7
      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: latest
      - name: Set up Python
        run: uv python install 3.11
      - name: Sync dependencies
        run: uv sync --extra dev
      - name: Ruff
        run: uv run ruff check src tests scripts tools
      - name: Mypy strict
        run: uv run mypy --strict src
      - name: Validate feature contracts
        run: >-
          uv run python scripts/validate_round28_manifest.py
          spec/features.yaml
          spec/source_file_map.yaml
          spec/contract_map.yaml
      - name: Dependency audit
        run: uv run pip-audit
      - name: Build wheel and source distribution
        run: uv build
      - name: Smoke-test the built wheel
        run: |
          uv venv --python 3.11 .wheel-smoke
          uv pip install --python .wheel-smoke/bin/python dist/*.whl
          .wheel-smoke/bin/python -c "import graph_agent; print(graph_agent.__name__)"

  runtime-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    env:
      UV_PYTHON: ${{ matrix.python-version }}
    steps:
      - uses: actions/checkout@v7
      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: latest
      - name: Set up Python
        run: uv python install ${{ matrix.python-version }}
      - name: Sync dependencies
        run: uv sync --extra dev
      - name: Run tests
        run: >-
          uv run pytest --tb=short -q
          --cov=src/graph_agent
          --cov-report=xml:coverage-runtime.xml
          --cov-report=term
      - name: Upload runtime coverage to Codecov
        if: matrix.python-version == '3.11'
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: coverage-runtime.xml
          flags: runtime
          fail_ci_if_error: false

  cross-platform-smoke:
    runs-on: ${{ matrix.os }}
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest]
    env:
      UV_PYTHON: "3.12"
    steps:
      - uses: actions/checkout@v7
      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: latest
      - name: Set up Python
        run: uv python install 3.12
      - name: Sync dependencies
        run: uv sync --extra dev
      - name: Run tests
        run: uv run pytest --tb=short -q
